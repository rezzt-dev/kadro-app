<Window x:Class="craftingTask.view.windows.windows_dialogs.task_dialogs.ShowTaskArchive"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:craftingTask.view.windows.windows_dialogs.task_dialogs"
        mc:Ignorable="d"
        Title="ShowTaskArchive" Height="600" Width="600" WindowStartupLocation="CenterScreen" AllowsTransparency="True" WindowStyle="None" Background="{x:Null}">
  <Border Background="{StaticResource MainBackground}" CornerRadius="12">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="22"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <Grid x:Name="titleBar" Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="22"/>
          <ColumnDefinition Width="22"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="minimizeButton" Grid.Column="1" Width="12" Height="12" Margin="4,6,6,4" 
                Click="btnMinimizeWindowClick" Style="{StaticResource MinimizeWindowButton}">
          <Viewbox>
            <Path Stroke="Black" StrokeThickness="2" VerticalAlignment="Center" Data="M2,7 L14,7" Width="16" Height="14"
                  HorizontalAlignment="Center" Opacity="0.5"/>
          </Viewbox>
        </Button>
        <Button x:Name="closeButton" Grid.Column="2" Width="12" Height="12" Margin="4,6,6,4" 
                Click="btnCloseWindowClick" Style="{StaticResource CloseWindowButton}">
          <Viewbox>
            <Canvas Width="16" Height="14" Margin="2" Opacity="0.5">
              <Path Stroke="Black" StrokeThickness="2" Data="M2,2 L14,12" />
              <Path Stroke="Black" StrokeThickness="2" Data="M14,2 L2,12" />
            </Canvas>
          </Viewbox>
        </Button>
      </Grid>

      <Grid x:Name="content" Grid.Row="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="5" CornerRadius="12">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="0.3*"/>
              <RowDefinition Height="*"/>
              <RowDefinition Height="0.15*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
              <TextBlock Grid.Row="0" Text="LISTA DE TAREAS ARCHIVADAS" FontSize="24" FontWeight="DemiBold" Foreground="{StaticResource MainForeground}"
                         VerticalAlignment="Bottom" Margin="15,0,0,5"/>
            </Grid>

            <Grid Grid.Row="1">
              <ListBox x:Name="taskListBox" ItemsSource="{Binding ArchiveTaskList}" DisplayMemberPath="Title" SelectionMode="Multiple" Margin="10"
                       Style="{StaticResource GenericDarkListBoxStyle}" ItemContainerStyle="{StaticResource DoneTaskDarkListBoxItemStyle}">
                <ListBox.Resources>
                  <Style BasedOn="{StaticResource DarkScrollBarStyle}" TargetType="ScrollBar" />
                </ListBox.Resources>
              </ListBox>
            </Grid>
          </Grid>
        </Border>

        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <Button x:Name="btnUnarchiveTasks" Content="Desarchivar Tareas" Style="{StaticResource FunctionButtonStyle}" Margin="4,4,2,-1"
                  VerticalAlignment="Center" Padding="0,4" Grid.Column="0" Grid.Row="0" Click="btnUnarchiveTasks_Click"/>
          <Button x:Name="btnDeleteTasks" Content="Borrar Tareas" Style="{StaticResource FunctionButtonStyle}" Margin="2,4,4,-1"
                  VerticalAlignment="Center" Padding="0,4" Grid.Column="1" Grid.Row="0" Click="btnDeleteTasks_Click"/>
          <Button x:Name="btnCancelCreation" Content="Cancelar" Style="{StaticResource FunctionButtonStyle}" Margin="4,-1,4,4"
                  VerticalAlignment="Center" Padding="0,4" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Click="btnCancelCreation_Click"/>
        </Grid>
      </Grid>
    </Grid>
  </Border>
</Window>
