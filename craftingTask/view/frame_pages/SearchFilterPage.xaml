<Page x:Class="craftingTask.view.frame_pages.SearchFilterPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:craftingTask.view.frame_pages" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:persistence="clr-namespace:craftingTask.persistence" Title="SearchFilterPage" d:DesignHeight="900" d:DesignWidth="1400" mc:Ignorable="d">
  <Page.Resources>
    <persistence:PriorityToStringConverter x:Key="PriorityToStringConverter" />
  </Page.Resources>
  <Border Background="{StaticResource SecondaryBackground}" CornerRadius="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="30"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Grid x:Name="titleBar" Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="35"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="btnCloseFrame" Grid.Column="2" Width="15" Height="15" Margin="3,3,13,-7" HorizontalAlignment="Right" VerticalAlignment="Center" Click="btnCloseSearchFilterFrame" Style="{StaticResource CloseFrameButton}">
          <Viewbox>
            <Canvas Width="16" Height="14" Margin="2" Opacity="0.5">
              <Path Data="M2,2 L14,12" Stroke="Black" StrokeThickness="2"/>
              <Path Data="M14,2 L2,12" Stroke="Black" StrokeThickness="2"/>
            </Canvas>
          </Viewbox>
        </Button>
      </Grid>
      <Grid x:Name="content" Grid.Row="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
          <ScrollViewer.Resources>
            <Style BasedOn="{StaticResource DarkScrollBarStyle}" TargetType="ScrollBar"/>
          </ScrollViewer.Resources>
          <Border Margin="10" CornerRadius="12">
            <StackPanel>
              <TextBlock Margin="15,20,15,10" FontSize="28" FontWeight="Bold" Foreground="{StaticResource MainForeground}" Text="BÚSQUEDA AVANZADA"/>
              <Grid Margin="15,10">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="PALABRA CLAVE"/>
                <TextBox x:Name="txtKeyword" Grid.Row="1" Height="35" FontSize="16" Style="{StaticResource TaskTitleDarkTextBox}" TextChanged="txtKeyword_TextChanged"/>
              </Grid>
              <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="10"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="PRIORIDAD"/>
                  <ListBox x:Name="lstPriorities" Background="{StaticResource SecondaryBackground}" Foreground="{StaticResource MainForeground}" ItemContainerStyle="{StaticResource ModernDarkListBoxItemStyle}" SelectionMode="Multiple" Style="{StaticResource ModernDarkListBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="ESTADO"/>
                  <ListBox x:Name="lstStatus" Background="{StaticResource SecondaryBackground}" Foreground="{StaticResource MainForeground}" ItemContainerStyle="{StaticResource ModernDarkListBoxItemStyle}" SelectionMode="Multiple" Style="{StaticResource ModernDarkListBoxStyle}"/>
                </StackPanel>
              </Grid>
              <Grid Margin="15,40,15,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="10"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="FECHA INICIO"/>
                  <DatePicker x:Name="dpStartDate" Height="35" FontSize="16" Style="{StaticResource SimpleDarkDatePickerStyle}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="FECHA FIN"/>
                  <DatePicker x:Name="dpEndDate" Height="35" FontSize="16" Style="{StaticResource SimpleDarkDatePickerStyle}"/>
                </StackPanel>
              </Grid>
              <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="10"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="PANEL"/>
                  <ListBox x:Name="lstPanels" Background="{StaticResource SecondaryBackground}" DisplayMemberPath="Name" Foreground="{StaticResource MainForeground}" ItemContainerStyle="{StaticResource ModernDarkListBoxItemStyle}" SelectionMode="Multiple" Style="{StaticResource ModernDarkListBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="TABLERO"/>
                  <ListBox x:Name="lstBoards" Background="{StaticResource SecondaryBackground}" DisplayMemberPath="Name" Foreground="{StaticResource MainForeground}" ItemContainerStyle="{StaticResource ModernDarkListBoxItemStyle}" SelectionMode="Multiple" Style="{StaticResource ModernDarkListBoxStyle}"/>
                </StackPanel>
              </Grid>
              <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="2*"/>
                  <ColumnDefinition Width="10"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                  <TextBlock Margin="0,0,0,5" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource MainForeground}" Text="FILTROS GUARDADOS"/>
                  <ComboBox x:Name="cmbSavedFilters" Height="35" DisplayMemberPath="Name" FontSize="16" SelectionChanged="cmbSavedFilters_SelectionChanged" Style="{StaticResource DarkSimpleComboBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Margin="0,0,0,5" FontSize="16" Text=" "/>
                  <Button x:Name="btnSaveFilter" Height="35" Click="btnSaveFilter_Click" Content="Guardar Filtro" Style="{StaticResource FunctionButtonStyle}"/>
                </StackPanel>
              </Grid>
            </StackPanel>
          </Border>
        </ScrollViewer>
        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Button x:Name="btnClear" Grid.Column="0" Margin="10,4,5,4" Click="btnClear_Click" Content="Limpiar" Style="{StaticResource FunctionButtonStyle}"/>
          <Button x:Name="btnSearch" Grid.Column="1" Margin="5,4" Click="btnSearch_Click" Content="Buscar" Style="{StaticResource FunctionButtonStyle}"/>
          <Button x:Name="btnClose" Grid.Column="2" Margin="5,4,10,4" Click="btnCloseSearchFilterFrame" Content="Cerrar" Style="{StaticResource FunctionButtonStyle}"/>
        </Grid>
      </Grid>
    </Grid>
  </Border>
</Page>
